# NeuG workload runners

See `/root/workspace/IncGQ/IncGQ-experiment-docs/NeuG/README.md` for the
current runner usage.

The actual directory is `NeuG` with an uppercase `N`. Do not create or use a
separate lowercase `neug` docs directory.

The current runner is a local NeuG build baseline runner. It uses the Python
API and shared libraries from `/root/workspace/neug` and
`/root/workspace/neug/build`, not the installed `/usr/local` Python package.
